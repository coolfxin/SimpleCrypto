# SimpleCrypto
A simple app for encrypt files on Android.一个简单的文件加密应用，可运行在安卓手机上

### 编译方法
将项目目录拉取到本地后，在AndroidStudio中打开，并同步依赖项后，直接编译运行即可

### 当前已知问题
1.加密密码修改后，将导致使用旧密码加密的文件解密失败。（软件默认的逻辑为加解密都直接使用当前的加密密码）
2.超过1Gb的视频文件加解密可能会失败
